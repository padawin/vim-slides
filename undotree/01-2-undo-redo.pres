# 01


                             # Undo/Redo changes #


* The `u` key

    Undo the last change. Goes one position back in modifications history.

* the `CTRL-R` key

    Redo the next change in the changes history.
